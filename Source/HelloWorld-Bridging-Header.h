// No include guard here on purpose. Swift's ClangImporter compiles this file
// as a translation unit's main file rather than including it, so a #pragma once
// draws -Wpragma-once-outside-header — an error under
// GCC_TREAT_WARNINGS_AS_ERRORS. Nothing includes this header, so it needs no
// guard; HelloWorld.hpp carries its own.

#include "HelloWorld.hpp"
